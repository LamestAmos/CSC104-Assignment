## Question 5

Build a **real-time registration form validator** using regex. The form
must validate: a username (alphanumeric, 5–15 characters), a
password (min 8 characters, at least one uppercase letter, one digit,
and one special character), and a Nigerian phone number (`+234`
followed by 10 digits). Show live feedback beside each field as the user
types.
